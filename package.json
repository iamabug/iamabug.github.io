{
  "name": "iamabug-github-io",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "sync:pages": "npm run build && cp dist/index.html index.html && cp dist/.nojekyll .nojekyll",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.4.2"
  }
}
